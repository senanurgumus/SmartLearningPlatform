specVersion: "v1"
serviceId: "smart-learning"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "smart-learning-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
